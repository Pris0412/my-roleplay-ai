# Mac 使用说明（只看这一页）

## 第一次

1. 打开分支页面 → 右上角绿色 **Code** → **Download ZIP**，解压到桌面。
2. 打开解压出来的文件夹 → 进 `server` → **右键** `start.command` → **打开** → 再点一次 **打开**（第一次 macOS 会拦，以后双击就行）。
3. 黑色窗口会自动下载 Node（约 50MB）和依赖，等一两分钟。
4. 出现「第一次需要登录」的提示 → 按任意键 → 出现 Claude Code 界面：
   - 输入 `/login` 回车；
   - 浏览器会打开，用你的订阅账号登录，把授权码粘贴回黑色窗口回车；
   - 看到登录成功后，按两次 `Ctrl+C`，脚本自动继续。
5. 浏览器自动打开 http://localhost:8787 。输入框旁的下拉框就是型号列表（Opus 5 / Sonnet 5 / Haiku 4.5 …）。

## 以后每次

双击 `server/start.command`，等浏览器打开就行。关掉黑色窗口 = 关掉后端。

## 手机也想玩

电脑保持后端开着，手机连同一个 Wi-Fi，浏览器打开 `http://<电脑IP>:8787`。
电脑 IP 看：系统设置 → Wi-Fi → 详细信息 → IP 地址。

## 出问题

- 页面提示「后端还没登录」：删除 `server/data/.logged-in`，重新双击。
- 想换账号：同上。
- 其他情况：把黑色窗口最后几行截图发给我。
