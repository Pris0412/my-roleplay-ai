# 提灯酒馆 (Lantern Tavern)

一个类似 SillyTavern 的 AI 角色扮演聊天页面，作为 claude.ai Artifact 发布。

- `index.html` — 整个应用（单文件，无构建步骤）。发布时由 Artifact 工具包上 `<!doctype html>` 外壳。
- 对话通过 Artifact 的 `sample` 能力调用 Claude，由查看者自己的账号付费；第一次发送会弹出授权。
- 角色、聊天记录、人设、设置全部存在浏览器 localStorage（`lantern-tavern.v1`）。

## 已有功能（v1）
- 角色卡：名字、标签、头像（表情/颜色/上传图片）、描述、性格、场景、开场白、对话示例
- `{{user}}` / `{{char}}` 占位符替换
- 用户人设（名字 + 描述）
- 流式回复、停止、重新生成、编辑/删除/复制消息、空消息发送 = 让角色继续
- 模型档位 / 回复长度 / 回复语言 / 上下文条数
- 深色优先，跟随系统主题

## v2 · SillyTavern 角色卡兼容
- 导入 `.png`（tEXt `chara` / `ccv3` 块）和 `.json`（v1 / v2 / v3），支持多选和拖入左栏
- PNG 卡的图片自动成为头像
- 字段映射：name / description / personality / scenario / first_mes / mes_example / alternate_greetings /
  creator_notes / system_prompt / post_history_instructions / tags / creator / character_version / character_book / extensions
- `system_prompt` 会替换默认扮演规则，支持 `{{original}}`；`post_history_instructions` 放在对话末尾
- 世界书（character_book）基础版：常驻条目总是带入，其余按关键词在最近几条消息里扫描；`selective` + `secondary_keys` 也认
- 备用开场白：开场白消息上可以切换
- 导出 V2 JSON，或导出 PNG（同时写入 `chara` v2 和 `ccv3` v3 块）；导出需要 Artifact 的 `downloads` 能力

## v3 · 正则脚本、Markdown/HTML 渲染、左右滑动
- 兼容 SillyTavern Regex 扩展的脚本格式（scriptName / findRegex / replaceString / trimStrings / placement /
  markdownOnly / promptOnly / substituteRegex / minDepth / maxDepth）
- 全局脚本在设置栏管理（导入 JSON、新建、编辑、启停、导出）；角色卡自带的 `extensions.regex_scripts` 随卡导入导出，在编辑角色的高级区管理
- 编辑器里可以贴一段文本实时预览替换结果
- 消息经过：占位符替换 → 正则（显示阶段）→ Markdown（marked）→ 消毒（DOMPurify，允许 style）→ 渲染到 Shadow DOM，卡片自带 CSS 不会污染页面
- 提示词阶段单独跑一遍正则（markdownOnly 的不跑，promptOnly 的只在这里跑）
- 回复支持多版本：‹ n/N ›，最后一条回复按 › 生成新版本；开场白在多个开场白之间循环切换
- 库从 cdnjs 加载，加载不到时退回纯文本渲染

## v3.1 · 前端卡渲染修复、宏、删除角色
- 渲染前先把成对的块级 HTML（div / table / details / style …）整体保护起来，再交给 marked，空行和缩进不再被拆成代码块
- DOMPurify 开 `FORCE_BODY`，开头的 `<style>` 不再被丢掉
- 常用宏：`{{persona}}` `{{description}}` `{{scenario}}` `{{time}}` `{{date}}` `{{random}}` `{{pick}}` `{{roll}}` `{{newline}}` `{{//注释}}`
  `{{lastMessage}}` 系列、`{{setvar/getvar/addvar/incvar/decvar}}` 和 global 版本
- 角色列表悬停出现 ✕，编辑角色底部有「删除角色」，设置栏原有的按钮保留
- 渲染库加载失败时页面顶部会提示

## v4 · 世界书（World Info / Lorebook）
- 同时接受 V2 卡的 `character_book` 和 SillyTavern 原生世界书 JSON（`entries` 为对象或数组都行）
- 两级：角色世界书随卡导入导出（V2 格式）；全局世界书在设置栏管理（导入 / 新建 / 启停 / 删除），导出为 ST 原生格式
- 条目编辑器：标题、关键词（支持 `/正则/`）、次要关键词 + 四种逻辑、内容、常驻、区分大小写、整词匹配、
  插入位置（角色设定前 / 后）、顺序、概率、扫描深度、递归相关开关
- 触发逻辑：常驻恒带；关键词扫描最近 N 条；次要词逻辑 AND ANY / NOT ALL / NOT ANY / AND ALL；概率；
  递归扫描（世界书级开关 + 条目级 exclude / prevent）；禁用条目跳过；按 token 预算裁剪，order 高的优先保留
- 前置条目插在角色设定之前，其余在之后

## v4.1 · 按名字绑定的角色世界书
- 识别卡里的 `extensions.world`（SillyTavern 的「角色世界书」只存名字），编辑角色里可以看到并改绑
- 同名的全局世界书对该角色始终生效，即使全局未勾选；导出时写回 `extensions.world`
- 导入角色卡的提示会列出：正则几条、世界书是否嵌入、绑定了哪本、是否已关联

## v4.2–4.3 · 字数区间、灵感、抢话方式
- 回复长度改为字数区间：短 200–300 / 中 500–800 / 中长 800–1200 / 长 1200–2000 / 自定义
- 「灵感」按钮：按你的人设 + 最近对话，用 `sample.json` 生成 3 条不同走向的草稿，点一下填进输入框；输入框里已有的半句会作为草稿方向
- 「抢话」快速切换（输入框上方）：完全不抢话 / 不抢话但可写我的表情动作 / 可复述我的话 / 复述 + 扩展动作表情，直接改提示词里的对应规则

## v4.4 · 角色可挂多本世界书
- 角色世界书从单本改为列表：新建、导入到角色、逐本启停、逐本删除、编辑条目
- 导出角色卡时把启用的几本合并成一本 `character_book`
- 修复：世界书的启用 / 禁用状态在重开编辑器或刷新后丢失；卡里已嵌入同名世界书时不再提示「需另外导入」

## v4.5 · 记忆总结
- 每条消息显示楼层号 `#n`
- 「记忆」按钮（顶栏 / 设置栏）：选楼层范围（默认从上次总结的下一楼到最新），生成总结，可改后存为条目
- 首次保存自动创建绑定该角色的「记忆 · 角色名」世界书（`memory: true`），条目按 `起-止` 楼命名，常驻，插在角色设定之后
- 总结提示词可自定义并保存，占位符 `{{char}} {{user}} {{range}} {{messages}} {{previous}}`；此前的记忆会作为衔接喂给模型
- 可选：带入上下文时跳过已总结的楼层（保留最近 4 条）

## v5 · 记忆索引、上下文用量、楼层隐藏
- 页内全文检索（不依赖任何外部 API / WASM）：分词倒排索引 + BM25，中文按双字切、英文按词；索引从聊天记录即时重建
- 输入框右下角的「上下文」按钮显示本次会发送的大小（占 64 KiB 上限的比例）、楼层范围、隐藏数、检索数；超过 75% 或有楼层被丢弃时变红
- 上下文面板：分项大小、建议、「隐藏第 1–N 楼」批量隐藏、取消全部隐藏；每条消息也有「隐藏 / 取消隐藏」
- 隐藏的楼层不进上下文，但仍在记录里、仍可被检索
- 自动检索：发消息时用最新一条消息去搜不在上下文里的楼层（含隐藏），最多带入 5 / 10 / 20 / 30 楼，
  以「相关记忆」附在角色设定之后；过滤掉泛词，只保留相关度达到最高分 30% 的命中
- 手动搜索：面板里输入关键词，命中高亮，点结果跳到对应楼层
- 说明：Artifact 的 CSP 拦掉了 SQLite 的 WASM 下载，所以用同等原理的 JS 实现代替 FTS5

## v6 · SillyTavern 聊天补全预设
- 导入 ST「聊天补全」预设 JSON（含 `prompts` / `prompt_order`），多选；设置栏选择当前预设或「内置默认」
- 按 prompt_order（优先 character_id 100001）拼装：文本提示词做宏替换；标记替换为实际内容
  （worldInfoBefore / charDescription / charPersonality / scenario / personaDescription / worldInfoAfter / dialogueExamples / chatHistory）
- `wi_format`、`scenario_format`、`personality_format`、`names_behavior` 生效；角色卡的 system_prompt / post_history_instructions 覆盖 main / jailbreak，支持 `{{original}}`
- `injection_position = 1` 的提示词按 `injection_depth` 插进历史；assistant 角色保留为 assistant 轮；system 作为 user 发送
- 本页自己的规则（长度 / 语言 / 抢话）作为一条可移动、可禁用的「提灯酒馆 · 回复规则」列在顺序里；检索到的记忆挂在 worldInfoAfter 之后
- 提示词管理器：启停、上下移动、编辑（名称 / 角色 / 内容 / 位置 / 深度）、新建、移除、加回未列出的
- 导出为 ST 可读的 JSON（保留采样参数等原字段）；采样参数在页面里标为不生效
- 超出输入上限时优先丢最早的历史，不丢预设提示词

## v7 · 自定义 OpenAI 兼容 API、数据备份
- 设置栏「模型接口」：Claude（页内，无需密钥）或自定义 OpenAI 兼容 API（Base URL + Key）
- 「测试并读取模型列表」调 `GET /models`，下拉选模型，也可手填模型 ID
- `POST /chat/completions` SSE 流式（也接受非流式 / JSON 响应），`reasoning_content` 折叠为思考过程，停止 = AbortController
- 采样参数：temperature / top_p / max_tokens / 流式；可勾选「跟随当前预设」（含 frequency / presence penalty）
- 自定义接口下 system 角色原样发送；Claude 页内接口则映射为 user 并补首尾 user 轮
- 灵感 / 记忆总结 / 滑动生成都走同一入口 `complete()` / `completeJson()`
- 「数据」区：备份全部数据（JSON，不含 API Key）/ 恢复备份（覆盖前确认）
- 限制：在 Claude 的 Artifact 里打开时，浏览器 CSP 不允许页面访问任何外部地址（含 localhost），
  自定义接口需把 `index.html` 下载到本地用浏览器打开；页面会明确提示，并给出对应错误文案

## v7.1 · 全局库
- 世界书 / 提示词预设 / 正则脚本这三样「对所有角色生效」的东西从齿轮里移出，合并到单独的「全局库」界面（顶栏「全局库」、左栏底部、齿轮底部都有入口），分三个标签页
- 齿轮底部加「保存设置」按钮（设置本来就是即改即存，按钮用来确认并在窄屏上收起面板）

## v7.2–7.4 · 自定义条数、上下文窗口、聊天背景
- 「带入上下文的消息数」和「每次最多带入几楼」都可自定义（后者上限 100）
- 自定义接口下上下文上限跟随「上下文窗口（token）」设置（扣回复预留，按 1 token ≈ 3 字节估算）
- 顶栏 🖼 打开「聊天背景」：10 套内置 CSS 背景、上传图片（缩到 1920px、JPEG 存本地）、压暗 / 模糊 / 气泡不透明度滑杆；
  可对所有角色或只对当前角色生效，角色设置覆盖全局，可恢复默认

## v7.5 · 快捷输入条
- 输入框正上方一排按钮：「」“” *…* （）…… —— 等，点一下插到光标处；成对符号把光标留在中间，选中文字再点则包住
- 可自定义（`|` 标光标位，`标签=内容` 给长句起短名），前 9 个支持 Alt+1…9；可整条隐藏

## v7.6 · 酒馆助手式前端卡
- 自动拆开 ```html 代码块；识别整份 HTML 文档（`<!DOCTYPE>` / `<html>` / `<head>` / `<body>`）
- 内嵌模式（默认）：抽出 head 里的 `<style>` 和 body 正文，去掉 script / title / meta，渲染进消息气泡
- iframe 模式（齿轮 › 前端卡渲染方式，实验）：整份文档在 `sandbox="allow-scripts"` 的 srcdoc iframe 里运行，
  注入酒馆助手常用 API 替身：getChatMessages / getCurrentMessageId / getLastMessageId / getCharData / getVariables /
  replaceVariables / insertOrAssignVariables / setChatMessages / setChatMessage / triggerSlash（空）/ eventOn（空）/ toastr / SillyTavern.getContext；
  高度通过 postMessage 自适应；环境禁止 iframe 时自动退回内嵌
- 页面脚本里不能出现字面的注释开头序列，否则 HTML 解析器会吞掉整段脚本（已踩坑）

## v8 · 状态面板与心声
- 顶栏「状态」（编辑角色左边）打开面板：启用开关、字段（默认 好感度 / 关系 / 心情 / 时间 / 地点，可增删；数值 0–100 用滑杆）、心声、给模型的补充说明、提示词预览
- 启用后顶栏下方常驻状态栏：数值带进度条，文字项成胶囊，心声斜体；点状态栏也能打开面板
- 提示词里加「状态面板」段：当前值 + 要求模型在回复末尾输出 `<status>{...}</status>` 更新
- 回复完成后解析最后一个状态块并写回字段和心声；块在显示、复制、后续上下文里都被去掉；滑动切换版本时按该版本的块恢复
- 预设模式下挂在「提灯酒馆 · 回复规则」标记里

## v8.1–8.2 · 状态栏显隐、侧栏收起
- 状态栏可「收起」，面板里可再显示；与「启用」独立
- 桌面端左栏和右侧设置栏都可收起 / 展开（顶栏 ☰ 和 ⚙，或栏内 ✕），状态记住；窄屏仍是抽屉

## v8.3 · Token 明细
- 「🧠 上下文」按钮改为显示估算 token / 上限 token；面板首屏是 Token 明细表
- 分组：预设提示词（逐条）、固定指令（开场 / 规则 / 状态面板）、角色卡与人设、世界书与记忆书（逐条）、检索到的旧楼层、对话历史（逐楼）；每项字数、≈token、占比条；合计与上限
- 估算：CJK 1 字 ≈ 1 token，其余 3.8 字节 ≈ 1 token；自定义接口请求带 `stream_options.include_usage`，返回的 `prompt_tokens` 用于校准（滑动平均），并显示上次真实计费

## v9 · ➕ 菜单与手机聊天
- 输入框左边的 ➕ 打开玩法菜单（可扩展的 `PLUS_ITEMS`），第一项「手机」
- 手机是同一条时间线的另一种显示：手机消息仍是楼层，带 `channel: "phone"`；进入 / 放下手机各插一条 `kind: "divider"` 的场景说明楼
- 主聊天区把连续的手机楼层折成一张「手机聊天 · N 条」卡片，点开即手机界面；分隔楼显示为居中小标签
- 手机界面：iMessage 风格气泡、时间戳、正在输入、已送达、深浅色切换；「让 TA 先开口」「过一会儿…」两个主动触发；「收起」保持手机模式，「放下手机」退出
- 提示词：手机模式下用「手机聊天规则」替换长度 / 抢话规则（短句、逐行多条、不写动作、`[不回复]`）；历史里手机消息写成 `[手机 时间] 名字：内容`，分隔楼写成场景说明
- 记忆自动衔接：手机楼层照常进上下文、被索引检索、被记忆总结、更新状态面板；主输入框发送会自动放下手机
- v9.2：拿出手机前先选场景：「不在一处」或「同一场合，悄悄发」，可附一句情境补充（写进分隔线和提示词）。同场合模式下角色能提现场的人和事，可用全角括号单独一行写现场小动作，手机里显示为居中的斜体动作行；退出分隔线改为「收起手机，回到现场」
- v9.6：手机里的消息可以操作了：点一下气泡滑出「编辑 · 隐藏 · 删除 · 回溯到此」，最后一条 TA 的消息多「重新生成」；编辑在气泡原地进行，回车保存、Esc 取消；删除和回溯与主聊天共用同一套逻辑（状态面板回滚、手机开关跟分隔线）；分隔线不可编辑
- v18：📜 剧情灵感 + 📣 旁白。➕ 菜单「剧情灵感」：按世界书、人设、关系、状态、在场的人和最近 20 楼，让模型想 4 个事件（可选基调：甜 / 日常 / 误会 / 悬念 / 虐，可换一批）；每张卡有「这是什么」（像世界书条目那样写来历和讲究）、「可以玩什么」、2–3 条推荐剧情（标题 + 钩子 + 涉及谁 + 基调，按基调配色）；每条剧情三个按钮：让 TA 提起（下一条回复自然带出）/ 旁白插入（写一条旁白楼层并让 TA 接着演）/ 写进输入框；卡片上的「存进世界书」把事件描述存进角色的「剧情事件」世界书（关键词触发）。卡片本身不进上下文。➕ 菜单「旁白」：以叙述者身份插一条楼层（`narration:true`，显示为「旁白」虚线气泡，提示词里写成「[旁白] …」，总结里写成「旁白：…」），可勾选插入后让 TA 接着演；独处时算那个 NPC 的场景
- v20.2：建卡题材也有「自定义」输入框；细调里加「XP / 偏好提示」；XP 栏写细：恋爱风格 / 主动被动 / 占有欲 / 相处方式 / 亲密时的样子 / 偏好列表 / 雷点列表 / 表面与私下
- v20.3：可以当网站发布（GitHub Pages）。不在 Claude 里打开时：自动切到「自定义接口」，页内 Claude 选项灰掉，提示改成面向访客（填自己的 API、数据只在自己浏览器）；角色卡 / 预设 / 世界书导出改用浏览器普通下载；填 api.anthropic.com 时自动带 anthropic-dangerous-direct-browser-access 头，可直连官方
- v21：前端卡大修。iframe 模式下正文和卡按原位置一起显示，一楼可多张卡；只挂视野附近的 iframe，滚远了换成占位（长对话不卡）；酒馆助手 API 补齐：triggerSlash（/send /setinput /trigger /echo /sendas /sys /setvar /getvar /addvar /incvar /decvar /flushvar /hide /unhide，全局变量同名 global 版）、eventOn / eventOnce / eventEmit（message_sent / generation_started / message_received / generation_ended）、generate、消息级变量（getVariables type:message）、updateVariablesWith / insertVariables / deleteVariable；iframe 里注入酒馆主题变量（--SmartTheme*）跟随明暗；自定义 CSS：全局（设置 › 生成）+ 角色级（编辑 › 自定义 CSS）；「前端代码带入上下文」：默认只有最近 2 条回复带代码，更早的楼只留卡上的文字，记忆总结 / 检索 / 灵感永远只看文字；卡长高时不再把阅读位置顶跑
- v21.1：头像照片。「你的人设」可以上传自己的头像（对话、手机、朋友圈都显示）；联系人编辑里也能给每个 NPC 上传照片（对话楼层、手机卡片、通讯录、朋友圈都显示）；照片统一缩到 160px，很小；存储明细里能单独删
- v21.2：单个角色打包导出 / 导入。编辑角色 › 「打包导出（含记录）」把角色连同聊天记录、记忆书、世界书、联系人、存档、状态、关系、绑定的全局世界书打成一个 .pack.json；用「导入角色卡」导回来，遇到同一份会问覆盖（先自动留存档）还是另存副本
- v21.3：搜索记录。顶栏 🔍（或 Ctrl/⌘+F）在这个角色的全部记录里找字句：正文、手机、朋友圈、已隐藏楼层都算，命中高亮，点结果跳到那一楼；没有完全匹配时退回记忆索引给出相近的楼层
- v20.1：建卡去掉「尺度」选项（XP 栏固定写法：恋爱风格 / 偏好 / 雷点，不写露骨过程，尺度看剧情）；外貌类型多了「自定义」，选中出现输入框自己写
- v20：AI 建卡 v2。入口只需「题材芯片 + 一句话 + 🎲 随机来一张」（细调、NPC 提示、建卡偏好都收在折叠里）；主角性别、尺度（清水 / 暧昧 / 成人）、外貌类型（男：清冷禁欲 / 阳光少年 / 斯文败类 / 野性痞帅 / 贵气矜贵 / 温润如玉 / 冷硬军人 / 妖孽 / 病娇美人；女：明艳 / 清纯 / 冷艳 / 娇软 / 英气 / 温婉 / 妩媚 / 少年感 / 高岭之花）。两次调用：① 主角卡 YAML 十五栏（基本 / 外貌分项 / 财富 / 性格 / 喜好 / 经历 / 关系 / 称呼 / XP / 说话 / 手机 / 朋友圈 / 底线 / 钩子 / 作息）+ 标签 / emoji / tags / 开场场景 / 2–3 个开场白 / 写作注意事项 / 状态初值；② 世界书（背景、人物关系、地点、势力）+ NPC（精简 YAML 七栏、和主角及你的关系与好感）。每栏可改可重刷，「换一套」整张重来。创建：描述为 YAML，注意事项进对话后指令，开场白多版本存备选，状态面板填关系 / 好感 / 地点 / 时间 / 心情 / 穿着，绑世界书，NPC 进通讯录并填关系。「建卡偏好」文本框（`settings.wizPrefs`，默认：无疤无痣、禁雪松、香水按家境、消费品可用现实品牌而地名机构虚构、男帅女美类型各异、富人细节到牌子）。内置全局世界书「顶奢参考（内置）」：服装 / 手表珠宝 / 车与出行 / 香水气味 / 住与生活方式 / 富人写法规则六条，关键词触发，可编辑；建卡时喂给模型
- v19：角色栏「✨ 让 AI 帮我建一张卡」：填几句提示（主角名字 / 性别 / 年龄 / 身份 / 大概性格 / 家境 / 和你的关系 / 外貌风格多选 / 世界背景 / 你的人设 / NPC 每行「性别 + 一句话」），一次调用生成：主角（标签、外貌含身高、性格、家境背景、说话方式、开场场景、开场白、emoji）、世界书（世界名、背景总述、地点、势力）、NPC（名字、性别、年龄、身高、外貌、性格、家境、说话方式、关系）。每块可改、可单独重刷（带外貌风格偏好）。「创建角色卡」建角色 + 绑一本「世界书 · 世界名」（背景常驻，地点 / 势力 / 人物按名字触发）+ NPC 进通讯录（含性格、说话方式、和主角的关系）。世界强制虚构：不用现实地名 / 学校 / 公司 / 人物，基调可贴近现实
- v18.1：剧情灵感打开时不再自动请求，选好基调点「✨ 开始想」才请求；基调多了「搞笑 / 抽象」（推荐剧情配青色）
- v18：➕ 菜单新增「📜 剧情灵感」：按世界书、人设、关系、状态、在场的人和最近 20 楼，让模型想 4 个事件（可选基调、换一批），每张卡有「这是什么」（像世界书条目的来历和讲究）、「可以玩什么」、2–3 条推荐剧情（标题 / 钩子 / 涉及谁 / 基调配色）；每条推荐剧情可「让 TA 提起」「旁白插入」「写进输入框」；卡片可「存进世界书」（角色的「剧情事件」世界书，按事件名触发）。旁白楼层：`narration:true` 的 user 楼，显示为「旁白」虚线气泡，提示词里写成 [旁白]，独处时归那个 NPC 的场景；只通过事件卡的「旁白插入」产生，不单独露出入口
- v17：最后一楼的操作行（编辑 · 重新生成 …）里多了「再写一段」：你不说话，让主角（独处时是那个 NPC）接着往下演一段，只加一条 TA 的楼层，不占你的楼层；提示模型不重复、不替你说话；手机模式下提示用手机里的按钮
- v16.6：设置「生成」里新增「阅读宽度」：窄 760 / 中 860（默认）/ 宽 1100 / 更宽 1400 / 满，控制聊天区、分隔线、输入区的最大宽度（`--read-w`），大屏全屏时不再两边大片留白；手机不受影响
- v16.5：修复：窗口在 761–1080 宽时收起角色栏，页面右边会空出一条给（其实是浮层的）设置抽屉的位置，顶栏按钮被挤成竖排。现在这个宽度下收起角色栏就是全宽；顶栏按钮任何宽度下不再被压扁，放不下就换行
- v16.4：手机宽度过了一遍：页面不再横向溢出（顶栏按钮在窄屏自动换到第二行、全部可见；抽屉收起时不撑宽页面）；输入框工具胶囊变紧凑，快捷符号一行横向滑动；记忆窗口三个一键按钮排成一行；弹窗标题行的按钮不再折行；总览行的小字换行显示；存储明细的「全局」一栏名字缩短
- v16.3：去掉输入框下面那行灰字说明（星号动作 / 宏替换 / 拖入角色卡），右侧的模型档位 / 接口状态保留
- v16.2：「🧠」按钮丢楼时仍变红，但直接写「丢了 N 条 · 建议带入 M 条」；索引面板的提示说明白是消息数和回复长度不匹配、隐藏更早楼层没帮助，并给一个「改成带入 M 条」按钮一键改；塞得下的不足 8 条时提示去看预览 / 大总结。索引面板底部加「保存设置」
- v16.1：在场标记不用手动：还没标过时，提示词让模型在下一条回复末尾报一份完整名单 `<cast>在场：阿桥、司晨阳</cast>`（或「在场：无」），页面照单建立名单；之后模型继续报来去。弹框里新增「🤖 让模型判断」：读最近 20 楼判断谁在场，自动勾好，确认即可
- v16：在场标记。输入框上方多了「👥 在场：…」小标签（有通讯录人物时显示；独处 / 手机模式下隐藏）：点开勾选此刻在场的通讯录人物，页面算出谁来了谁走了，写成一条「👥 阿桥 来了；司晨阳 走了」分隔线（`kind:"divider", cast:true, present:[ids]`），任一楼的在场名单 = 往上最近一条这种分隔线，没有时仍按名字猜。提示词里多了「## 现在在场」（在场者的简介 / 性格）和规则：不在名单的通讯录人物不要出现、路人可自由出现、有人来去时在回复末尾输出 `<cast>名字 来了；名字 走了</cast>`；页面收到后自动写分隔线并弹「撤销」，重生成会替换上一次的报告。标签在展示 / 历史 / 总结里都会被去掉。记忆「建议分段」在有标记之后按名单切段，不再猜名字。路人、群众不用标
- v15.3：设置抽屉分节顺序调整为：模型接口 → 你的人设 → 生成 → 存储 → 数据 → 当前对话（仍是一页，保存设置 / 删除这个角色照旧）
- v15.2：一键清理去掉「删已合并停用的旧记忆条目」一项，只剩删自动存档 / 压缩图片
- v15.1：占用明细改成按角色分组（收起时只看角色名 · 大小 · 几项，按大小排），每栏有「删除整个角色」，展开才是对话 / 存档 / 头像 / 记忆书的单行删除；最下面一栏「全局」
- v15：本地存储换成 IndexedDB（浏览器自带的大抽屉，额度按硬盘算，一般几百 MB 到几 GB），localStorage 只做小镜像 / 兜底：首次打开自动把旧数据搬过去，之后每次保存两边都写，超过 5 MB 时只靠 IndexedDB（`state.savedAt` 决定谁新）。设置里新增「存储」：进度条 + 「本页数据 X / 浏览器可用 Y」，Safari 下提示 7 天不打开可能被清；「占用明细 · 清理 · 清空」窗口：按大小列出每个角色的对话记录、每个存档、头像、背景图、每本记忆书 / 世界书、预设，每行可单独删（删对话会先自动存档）；「一键清理」勾选删自动存档 / 删已合并停用的旧记忆条目 / 压缩过大图片（默认不勾）；「清空全部」需输入「清空」，先强制下载备份，保留 API 设置。左下角状态行也显示已用 / 可用
- v14：➕ 菜单里手机相关只留一项「📱 手机」（朋友圈、联系人两项并入）：打开回到上次停的那一页（某个 NPC 的私聊 / 某个群 / 朋友圈 / 主角手机聊天），否则到主页（联系人页：朋友圈在最上，然后主角、NPC、群）。「放下手机」后下次从主页开始。主聊天里的折叠卡片仍然直达对应页面；和主角的手机模式（分隔线、手机聊天规则）不变
- v13.9：「当前对话」统计改成分行：总楼数字数 / 主线总结进度 / 其他未总结（最多列 3 个，其余「等 N 处」）/ 这次带入，不会随 NPC 变多而无限变长
- v13.8：「当前对话」那行写清楚：主线已总结到 / 主线未总结几楼；各 NPC、群还没总结的条数；这次带入「N 条（第 a–b 楼之间，X 的视角 / 手机模式）」——条数和范围分开，独处时标明按谁的视角拼的上下文
- v13.7：设置里「当前对话」那行改成：共几楼几字 · 已总结到第几楼 · 未总结几楼约几字 / 几 tok · 这次带入第 a–b 楼约几 tok
- v13.6：去掉设置里的「带入上下文时跳过已总结的楼层（保留最近 4 条）」开关及其逻辑：和「带入上下文的消息数」重复，窗口由消息数控制，更早的靠记忆总结和检索
- v13.5：「⚡ 一键小总结」：只把还没总结的部分自动归位——主线未总结楼层按「谁在场」切段进主角记忆书（只有主角 / 公开），各 NPC 未总结的私聊和当面剧情进 TA 的记忆书，各群未总结的群聊进群的记忆书；已总结的和隐藏楼层都不动，开始前列出会做哪几段
- v13.4：重建记忆可以自定义「每段几楼」（默认 20，记住上次的值 `settings.rebuildLen`），不管填多少，一段超过 40 KB 原文都会自动再切一刀（页内 Claude 单次 64 KB 上限）；新增「📚 一键大总结」：把每本记忆书（主角 / 各 NPC / 各群）里在用的条目各自压成一条大总结，范围不同的条目分开合并不混，旧条目停用并标「已合并」，角色卡自带的世界书和全局世界书不动，先自动存档，进度可停
- v13.3：「记忆」窗口多了「🧹 重建记忆」：勾选主线 / 各 NPC 私聊 / 各群（默认全勾），先自动存档「重建记忆前」，删掉勾选对象的旧总结（手写条目保留）、取消所有隐藏楼层，把楼层按「谁在场」切段、每段最多 20 楼，一段一次调用重新总结存进各自的记忆书，进度显示第 x / n 段，可随时停止。范围简化：只有主角和你 → 只有主角知道；有别人在场或有朋友圈 → 公开（「建议分段」也改成这两档，「主角 + 某某」仍可手动选）
- v13.2：记忆窗口（主角模式）多了「建议分段」：把还没总结的主线楼层按「谁在场」切段（楼层正文里提到通讯录 NPC 的名字 / 别名就算在场，离场后再留两楼缓冲，不足三楼的小段并入前一段；朋友圈楼层单独成段），每段给出建议范围：只有主角知道 / 主角 + 在场的 NPC / 公开，点「用这段」自动填好起止楼层和范围。新增范围「主角 + 某某」（存成 `memory_shared`），NPC 私聊时也能读到这条记忆；已按范围存过的楼层不再出现在建议里
- v13.1：记忆总结的范围里含朋友圈动态 / 评论时，「谁能看到」自动预选「公开」（手动改过就不再动），提示里标出含几条朋友圈
- v13：朋友圈。手机联系人页顶部和 ➕ 菜单都有入口。你、主角、通讯录里的 NPC 都能发动态、点赞、评论，全部公开：每条动态 / 评论存成一楼（`channel: "moments"`，`kind: post|comment`，`author: user|char|npcId`，动态带 `likes`），主聊天里折成「朋友圈 · N 条动态」卡片，回溯、检索、主角记忆总结、存档都照常；提示词里写成「[朋友圈 时间] 名字 发了一条动态：…（❤️ 谁）」「名字 评论了 X 的动态「…」：…」，主角和所有 NPC 都看得到（主角能看到你和 NPC 互相评论）。「🔄 刷新朋友圈」让随机 1–3 人（或指定一人）发新动态，贴合最近正文剧情，图片用「[图片：描述]」；「👀 看看谁来了」让大家对最新动态点赞 / 评论（可指定一人）；「发完自动来反应」开关（`settings.momentsAuto`，默认关）；删动态会连评论一起删。模型只输出 JSON 动作数组（post / like / comment），作者只限主角和通讯录里的人
- v12.3：点手机不再先弹「拿出手机」选场景，直接进手机（沿用上次的场景，首次为「不在一处」）；手机顶部状态行变成可点的「🚶 不在一处 ▾ / 🤫 同一场合，悄悄发 ▾」，点开切换场景、补充情境：还没发消息时直接改写刚才的分隔线，已经聊过则追加一条「📱 换个情况：…」分隔线
- v12.2：索引面板顶部多了「这次发了什么 · 预览」（默认收起，显示状态会记住）：按分组（固定指令 / 角色卡 / 世界书与记忆书 / 检索 / 对话历史）折叠展示这次会发给模型的每一段原文，只读；世界书条目按「书名 › 条目名」标出；另有「这次没带上的世界书 / 记忆条目」列表，标明原因（关键词没出现 / 不在 TA 的知情范围 / 条目已关闭 / 概率没抽中 / 预算不够）；「复制全文」复制整份提示词
- v12.1：「重开对话」改成两档：「只清对话」（记忆书、状态面板、关系、NPC、群都保留）/「全部归零」（对话、记忆书、记忆索引、状态面板、关系、NPC 记忆计数、手机、用量都清掉；角色设定、世界书、联系人名单、存档保留），两档都先自动留一份「重开前」存档
- v12：安全网三件：① 左下角「💾 备份全部」一键导出（角色、对话、NPC、群、记忆书、设置，不含 API Key），角色栏底部显示「上次备份 N 天前 / 从未备份」，超过 7 天且有 10 楼以上内容时变红并在打开页面时提醒一次（`state.lastBackup`）；② 顶栏「存档」：给当前进度起名存档，随时「读档」回到那条线（记录对话、状态面板、关系、NPC、群、记忆书、记忆索引；角色设定和世界书不动），读档前、回溯前、重开前各自动留一份「自动 ·」存档（同类只保留最新一份），可改名、删除（`c.saves`）；③ 回溯或删掉最后一楼后，覆盖了被删楼层的记忆总结（主角按 `memory_range`，NPC / 群按 `memory_msgs`）会问要不要一起删掉，「已总结到第 N 楼」也会退回去，不会再把回溯后新聊的楼层当成已总结
- v11.7：「抢话」多了「可以抢话 · 替我说几句、做点动作」：角色可以按你的人设替你补一两句对白或小动作推进剧情，但改变走向的大事仍留给你
- v11.6：「不抢话 · 可以写我的表情动作」「可以复述我的话」两档也改成要求句（每次至少写一两处你的神态 / 回复开头先复述你的话），四档力度一致
- v11.5：「抢话」下拉多了「无 · 不管，交给角色卡」（一句抢话规则都不发）；角色卡自带系统提示词时，抢话规则也照样单独发一行进「规则」（之前只有写了 `{{original}}` 才会带上，等于没生效）；「复述 + 扩展」改成要求句：回复开头先从角色视角把你刚说的话和动作重描一遍并补细节，再回应
- v11.4：角色栏：一句话标签太长时换行（最多两行，去掉 HTML 注释和标签）；角色栏不再出现横向滚动条，底部按钮过长省略号
- v11.3：手机工具栏重排：按钮不再竖排换行；群聊分两行（谁说 + ▶ 聊一轮 / ↺ 重来 · 总结群聊 · 成员 · 图标）
- v11.2：群聊工具栏多了「谁来说」下拉（大家 / 各成员），选了某人再点按钮变成「让 X 说」，只让 TA 接话 1–3 条
- v11.1：建群窗口的「🎲 起名」：按勾选的成员、身份、性格和世界背景让模型想 6 个群名，每点一次换一个，用完自动再想；换了成员会重新想
- v11：群聊。手机联系人页「＋ 建群」：群名 + 成员（主角、NPC 任选，你自己默认在）。群消息带 `group: gid, speaker`，群里每个人都算知道：主角在群里主线就看得到，NPC 私聊里按时间夹着「[群聊·群名] 名字：…」；不在群里的人完全不知道。「让他们聊一会儿」一次生成 3–6 条「名字：内容」（页面按成员名拆成各自的气泡，气泡上方标名字），可「重来一轮」；群里 NPC 发言末尾带关系状态块更新「和你」。群有自己的记忆书「记忆 · 群名」（`memoryGroup`），群成员都能看到，记忆窗口和总览里有群一项；主角楼层总结跳过群消息。主聊天里折成「群聊 · 群名 · N 条」卡片。删掉了「转述给主角」（公开记忆和群聊已覆盖它的用处）
- v10.14：上下文面板去掉进度条 / 上限 / 合计，改成「用量」：本对话累计输入 / 输出 / 调用次数（自定义 API 用接口真实用量，页内 Claude 估算，`c.usage`）、这次将发送多少、对话历史几楼；只有真丢了楼层才提示，底部按钮不再变红（丢楼时显示「丢了 N 楼」）；隐藏楼层单独成段
- v10.13：主线提示词多了「出场过的其他人物」：最近 30 楼里被提到的 NPC 各一行身份 / 性格 / 说话方式，主角代演时照此来；NPC 自己说话时性格再压成一句放在提示词最末尾（结尾最受关注）
- v10.12：NPC 多了「性格」「说话方式」两项：扫描时优先照搬世界书、没有就按剧情判断；关系页「补全性格」给缺的人补；卡片上显示，缺的标「没写性格」；NPC 提示词里单独成块并要求严格保持，不滑成通用温和腔
- v10.11：扫描结果默认不勾选，加「全选 / 全不选」和已勾选计数；「认人」因为只列新人，默认勾上
- v10.10：主角回复的操作栏多了「认人」：只扫最近 8 楼里新出现的人（已在联系人里的不列），勾上即成长期 NPC，关系图立刻有卡片
- v10.9：关系档案。每条关系多了「现状一句话」（模型评估 / 扫描时写，可手填）、阶段标签（默认 20 疏远 / 45 认识 / 70 朋友 / 90 亲近 / 100 亲密，阈值可改）和好感条，以及变化轨迹（每次变动记第几楼、从多少到多少、原因；NPC 状态块多了「原因」字段）。提示词里的关系行带阶段和现状。NPC 和关系随角色卡导出 / 导入（`extensions.lantern.npcs / rels`）
- v10.8：关系页改成卡片（每人一张：名字 / 身份 / 出场 / 编辑 / 删除，下面两行「和你」「和主角」），手机宽度不用横向滚动
- v10.7：关系图改成表格：每人一行（和你 / 和主角 的关系 + 好感，出场，编辑 / 删除），第一行主角↔你读状态面板；表上有「扫描人物」（同一个扫描，顺便让模型从世界书判断每人和主角的初始关系）、「＋ 新建」、「让模型更新关系」。随剧情变：NPC 私聊 / 独处时每次回复末尾带 `<status>{"关系","好感度"}</status>`（多人时按名字分组）更新「和你」；「和主角」每 N 楼（设置 relAuto，默认 20，0 关）自动重新评估
- v10.6：关系图。「状态」窗口分两个页签：状态面板 / 关系图。关系图：你在中间、主角在上、NPC 围一圈，连线写关系 + 好感（你↔主角读状态面板；你↔NPC、主角↔NPC 存在 `c.rels`）。「让模型更新」单独发一次 JSON 请求（主线 + 各 NPC 私下记录 + 记忆书）给每条关系打关系词和好感；也可手填、清除。关系写进主角和 NPC 的提示词（「## 人际关系」），手机联系人行显示「忘年交 · 66」
- v10.5：多人独处 + 记忆总览。联系人的「当面」先弹「谁在场」勾选（可多选），横幅有「在场的人」可换人；这段楼层带 `npcs: [ids]`，模型同时扮演在场的人（每人各自的设定），每个在场 NPC 的上下文和检索都包含这段。NPC 总结改按消息 id 记录已总结（`memory.npcDone`），一个人总结过的多人场景对其他在场的人也算已总结，条目带 `memory_shared` 让他们同样看得到（标签「+ 小满」）；删联系人时多人记录留给其他人。记忆窗口顶部多了总览：主角聊到第几楼 / 总结到第几楼 / 未总结几楼，每个 NPC 从第几楼开始 / 共几条 / 未总结几条，各自记忆书条数和 token，「该总结了」「建议合并」提示，「去总结」直接切换
- v10.4：记忆窗口布局退回 v10.2（一键版反而更绕）；楼号范围里含独处楼层时提示「这段是和 X 的独处，请在总结谁里选 X」
- v10.2：记忆窗口多了「合并已有的记忆条目（大总结）」模式：列出这本书的条目勾选（默认勾同一可见范围的），模型压成一条「合并 · 1-20 ~ 61-80」，旧条目停用并标「已合并」（不删，世界书里可重新启用）；合并有独立模板（{{entries}}）；窗口底部显示记忆书常驻条数和约 token，超过 1500 提示该合并；主角和各 NPC 的书各自合并
- v10.1：记忆窗口顶部多了「总结谁的记忆」切换（主角按楼层 / 各 NPC 的私聊 + 当面）；独处进行中点「记忆」直接对准当前 NPC
- v10：NPC 当面模式。联系人里每个 NPC 多一个「当面」：主聊天顶部横幅「和 X 单独在一起，主角不在场」，主输入框写的剧情带 `npc: id, channel: "scene"`，正文规则（长度 / 抢话 / 动作星号），气泡用 NPC 名字和「独处」标记；主角完全看不到；「结束独处」插分隔线；回溯跨过分隔线时自动恢复 / 关闭；灵感在独处时替你写给 NPC 的回复；上下文按钮在独处时显示 NPC 的用量。「总结私聊」把手机和当面一起总结进 NPC 的记忆书。记忆窗口多了「谁知道这条记忆」选择（主角 / 公开 / 某 NPC），群像戏总结选公开即可让所有 NPC 知道
- v9.9：每个 NPC 一本自己的记忆书「记忆 · 老陈」（`memory: true, memoryNpc: id`），私聊总结存进去，删联系人时一起删；旧版存在主记忆书里的 NPC 条目加载时自动搬过去。NPC 书里的条目只有「只有 TA 知道 / 公开」两种；主角记忆书保留三种。顺带修了启动时单个角色规范化出错会回落到空白状态的隐患：现在只跳过那个角色，不丢数据
- v9.8：NPC 记忆。私聊界面多了「总结私聊」（把这个 NPC 还没总结过的消息压成一条记忆，存进同一本记忆书，条目标 `lantern_scope: npc:<id>`，只带给这个 NPC）和「转述给主角」（模型把私聊要点写成你当面说的一段话，可改，发出后插进主聊天、主角回应）。记忆书条目多了「谁知道」：只有主角 / 公开 / 只有某 NPC，注入时按说话人过滤；NPC 总结有独立模板（多一个 {{npc}}）
- v9.7：NPC 私聊。手机里多了「联系人」页（➕ → 联系人，或手机工具栏的 👥）：主角 + 这张卡的 NPC 列表；「扫描人物」让模型从世界书、记忆书、最近聊天里找出人物（名字 / 别名 / 一句话身份 / 来源条目）勾选加入，也可手动新建、编辑、删除。NPC 私聊楼层带 `npc: id`，主聊天里折成「和 X 的私聊 · N 条」🔒 卡片；主角生成时完全看不到 NPC 楼层，NPC 生成时只看自己的楼层 + 世界书 + 自己的设定 + 用户人设，并按名字是否在剧情里出现过决定「已出场 / 初次联系」；检索按说话人过滤；记忆总结和灵感也跳过 NPC 楼层。记忆书范围标记、「主角知情」开关、NPC 状态面板留到下一阶段
- v9.6：手机里的消息可以编辑 / 隐藏 / 删除 / 回溯到此 / 重新生成（点气泡展开操作行）
- v9.5：每楼多一个「回溯到此」（删掉这楼之后的全部）。删楼 / 回溯后，如果被删的回复更新过状态面板，面板退回到仍在记录里的最近一份快照（没有就回初始值）；你手动改过的值比快照新时会先问一句；回溯跨过手机分隔线时手机模式跟着开关
- v9.4：曾加过手机转账（💲 / 【转账：100】 / 账单），实际使用中越做越乱，已整体移除；实现留在 git 历史里（dddb828…4603dbe）
- v9.1：分隔楼不再占楼号。楼号只数真正的消息（`floorOf` / `floorCount` / `idxAfterFloor` / `chatRange`），记忆总结范围、隐藏到第 N 楼、索引检索、上下文统计全部按新楼号；分隔线不会被隐藏

## 本地使用
1. 在 Artifact 里「数据 › 备份全部数据」
2. 下载本仓库的 `index.html`，双击用浏览器打开（或任意静态服务器）
3. 「数据 › 恢复备份」，然后在「模型接口」切到自定义 API，填地址、读取模型、选模型

## 后续计划
见对话记录，一步一步加。
